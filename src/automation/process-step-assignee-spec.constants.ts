/** Shown in API responses and logs while per-user assignee CRUD is phased out. */
export const PROCESS_TEMPLATE_STEP_ASSIGNEE_DEPRECATED_MESSAGE =
  'process_template_step_assignees is deprecated; set assigneeSpec on process template steps instead.';

export const PROCESS_TEMPLATE_STEP_ASSIGNEE_WRITE_BLOCKED_MESSAGE =
  'Creating or removing process_template_step_assignees is disabled (PROCESS_STEP_ASSIGNEE_SPEC_ENABLED=true); use assigneeSpec on template steps.';
