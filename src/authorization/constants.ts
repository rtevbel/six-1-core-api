/**
 * Metadata key used by the RequirePermissions decorator and AuthorizationGuard.
 *
 * This constant defines the key under which permission requirements are stored
 * as metadata on route handlers and controllers.
 *
 * @version 0.0.1
 */
export const REQUIRED_PERMISSIONS_KEY = 'required_permissions';

