/** Default: child summaries only (E4 `progress`), no embedded `runner` trees. */
export const PROCESS_RUNNER_DEFAULT_CHILD_DEPTH = 0;

/** Maximum nesting levels below the requested instance (inclusive of direct children). */
export const PROCESS_RUNNER_MAX_CHILD_DEPTH = 2;
